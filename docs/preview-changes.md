# 502 mL 维C身体乳上新与网站优化：预览交付

## 当前状态

- 分支：`feat/website-optimization-body-lotion`。
- 当前是本地代码修改和本机开发预览。未推送 GitHub，未创建 Vercel 预览部署，未正式上线。
- 新品预览：<http://localhost:3000/products/vitamin-c-body-lotion-502ml>
- 身体乳入口：<http://localhost:3000/collections/skincare?type=body-lotion>
- 首页：<http://localhost:3000/>
- 以上地址供当前电脑访问，需要保留本地服务运行。重新启动方式见 README。

## 已完成的修改

1. 新增一个独立产品 `vitamin-c-body-lotion-502ml`：502 mL / 17 FL OZ、**US$2.99／瓶、2 瓶起订**。目录现有 23 个产品、5 个独立身体乳。原有 22 个产品逐字段比对全部保持一致，包括 444 mL 维C身体乳的 SKU、图片、US$3.99 单价与 12 pcs MOQ。
2. 接入附件全部四张图片，顺序为主图、质地、卖点说明、双瓶角度；双瓶图仍表示同一款产品，单价不变。增加缩略图切换、放大对话框和打开完整图片的入口。图片保留原画面与标签，无裁切、拉伸或新增功效文字。
3. 生成 160、480、800、1254 px WebP 文件，四图共 16 个派生文件，总计 755,622 字节；800 px 主图为 20,740 字节。按视口选图，缩略图及目录图延迟加载，显式预留图片尺寸。
4. 新品支持整数数量询价，从 2 瓶开始，低于 MOQ 或非整数时禁用询价。2 瓶商品小计 US$5.98，12 瓶 US$35.88；明确运费与税费待确认。WhatsApp 链接复用现有号码，携带新品名称、官方产品链接、单价、MOQ 和选定数量。
5. 首页和桌面／手机导航补上 Body Lotion 与 Low MOQ Perfume 入口；目录增加身体乳筛选，精简重复的采购说明和按钮，让产品卡片更早出现。卡片突出容量、已知价格、MOQ 和询价。
6. 产品商业类型和定制按钮按明确资料显示。新品采用 Wholesale，不默认承诺品牌、库存、交期、免费样品或定制能力。未确认字段的显示统一为联系确认，清理旧产品混合参数中的内部 `[TO CONFIRM]` 标记；不改动原始产品事实。
7. 新品加入独立 title、description、canonical、站点地图和 Product / Offer 结构化数据，价格为每瓶 2.99 USD，最低数量为 2；不添加虚构 SKU、品牌、库存、运费、评分或评价。图片使用官方域名下的绝对静态地址。
8. 复用现有 React 页面组件生成 28 个页面的 HTML 正文，保留 SPA 和 Vercel 架构。按路由拆分目录、产品详情与低 MOQ 页面；生产构建排除预览调试脚本。
9. WhatsApp 点击仅产生 `whatsapp_click`，不再同时产生 `generate_lead`，不把点击视为已收到询盘。正式域名外不加载 GA4 网络脚本；统计报表需要区分历史点击口径与实际线索。
10. 更新 README 的真实启动方式、Windows 命令、页面范围、数据和素材管线。旧原型说明单独保留，产品资料来源保存在 `vitamin-c-502ml-product-facts.json`。

## 验证结果

- TypeScript 类型检查通过，包含预渲染脚本。
- 9 个测试文件、30 项测试通过，覆盖新品事实、MOQ 校验、询价编码、SEO、图片路径、点击统计和既有后端逻辑。
- 生产构建通过。构建仍提示主 JS 包约 513 KB，大于 500 KB 建议值；是非阻断的体积提示，后续可进一步拆分公共依赖。
- 原有 22 个产品全部字段与分支起点一致，新增产品仅一条，slug 无重复。
- 构建输出 28 个页面，均有正文、唯一 H1 和官方 canonical；站点地图包含全部 28 个页面。
- 新品原始 HTML 包含价格、2 瓶 MOQ、默认数量 2 和正确 Offer 数据；未发现原始 `[TO CONFIRM]` 占位标记或生产调试脚本。
- 桌面 1440 × 1000、手机 390 × 844 检查首页、身体乳目录、新品、原有 444 mL 身体乳、香水详情及低 MOQ 页面，未发现横向溢出或检查时已加载的图片错误。
- 已操作验证四图切换、手机／桌面放大与关闭、手机菜单和身体乳筛选；目录显示 5 款身体乳。
- 实际验证 1 瓶无效、12 瓶小计及 WhatsApp 草稿内容；只检查链接，未发送真实消息或邮件。
- 需要真实 Resend 凭据的外部邮件验证未执行；不影响本轮产品浏览与预览交付。

## 待补充参数

1. 供应商 SKU、品牌及品牌归属资料。
2. 完整 INCI 成分表、维C形式与浓度。
3. 香型、保质期／开封后使用期、详细使用说明。
4. 库存、发货交期、运费、税费与贸易条款。
5. 每箱瓶数、外箱尺寸、净重与运输重量。
6. 认证文件，以及图片中质地和功效描述的支持材料。
7. 样品政策、是否可做自有品牌／定制、可定制范围和对应 MOQ。

现有图片上的橙子、Vitamin C、质地和功效文案只作为供应素材内容展示，不据此推断香型、完整配方、有效浓度或保证效果。上述缺失项均未套用其他产品的条件。
